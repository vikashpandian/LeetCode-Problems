#include <iostream>
#include <vector>
#include <string>

using namespace std;

string convert(string s, int numRows) {
    vector<string> zz(numRows, "");
    for (int i = 0; i < s.length(); i++) {
        int n = i / numRows;
        int m = i % numRows;
    }
}

int main() {

    return 0;
}