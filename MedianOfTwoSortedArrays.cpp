#include <iostream>
#include <vector>

using namespace std;

double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
    int start1 = 0, end1 = nums1.size() - 1, start2 = 0, end2 = nums2.size() - 1;
    int mid1 = (start1 + end1) / 2, mid2 = (start2 + end2) / 2;
}

int main() {

    return 0;
}